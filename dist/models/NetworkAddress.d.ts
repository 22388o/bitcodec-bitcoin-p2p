/// <reference types="node" />
export declare type NetworkAddressVersion = {
    services: Buffer;
    ip: string;
    port: number;
};
export declare type NetworkAddress = {
    time: number;
    services: Buffer;
    ip: string;
    port: number;
};
